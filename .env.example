# jobreach environment variables
# The Agent SDK uses your existing Claude Code subscription — no Anthropic API key needed.
# Brave Search is no longer required — the Agent SDK's built-in WebSearch is used.

SUPABASE_URL=your_supabase_url_here
SUPABASE_ANON_KEY=your_supabase_anon_key_here
